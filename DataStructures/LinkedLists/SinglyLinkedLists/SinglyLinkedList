import Node

class SinglyLinkedList:
    def __init__(self):
        self.head=self.tail=None
        length=1
    def listLength(self):
        current=self.head
        count=0
        while current != None:
            count+=1
            current=current.getNext()
        return count
